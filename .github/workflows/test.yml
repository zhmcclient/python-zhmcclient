# This GitHub workflow will setup and run various kinds of tests with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: test

on:
  schedule:
    # The schedule event always (and only) runs on the master branch.
    - # cron (in UTC): minute hour day_of_month month day_of_week
      cron: '00 22 * * SAT'
  pull_request:  # When creating a PR targeting these branches
    branches:
    - master
    - stable_*
  push:  # When merging a PR targeting these branches (direct push is disabled)
    branches:
    - master
    - stable_*

jobs:

  set_matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.select_matrix.outputs.matrix }}
    steps:
    - name: "Select matrix"
      id: select_matrix
      # Select full matrix when scheduled or when releasing, and normal matrix
      # otherwise. The matrix is defined as a JSON string.
      # This technique documented in:
      #    https://stackoverflow.com/questions/65384420/how-to-make-a-github-action-matrix-element-conditional
      # TODO: Find a way to define this with less escapes.
      run: |
        if [[ "${{ github.event_name }}" == "schedule" || "${{ github.head_ref }}" =~ ^release_ ]]; then \
          echo "matrix={ \
            \"os\": [ \"ubuntu-latest\", \"macos-latest\", \"windows-latest\" ], \
            \"python-version\": [ \"3.8\", \"3.9\", \"3.10\", \"3.11\", \"3.12\", \"3.13.0-rc.1\" ], \
            \"package_level\": [ \"minimum\", \"latest\" ] \
          }" >> $GITHUB_OUTPUT; \
        else \
          echo "matrix={ \
            \"os\": [ \"ubuntu-latest\", \"macos-latest\" ], \
            \"python-version\": [ \"3.8\", \"3.9\", \"3.11\", \"3.12\" ], \
            \"package_level\": [ \"minimum\", \"latest\" ] \
          }" >> $GITHUB_OUTPUT; \
        fi
    - name: Show matrix in JSON
      run: echo '${{ steps.select_matrix.outputs.matrix }}'

  test:
    needs: set_matrix
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix: ${{ fromJson(needs.set_matrix.outputs.matrix) }}
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    env:
      PIP_DISABLE_PIP_VERSION_CHECK: 1
      PIP_NO_PYTHON_VERSION_WARNING: 1
    steps:
    - name: Set run type (normal, scheduled, release)
      id: set-run-type
      uses: actions/github-script@v7
      with:
        result-encoding: string
        script: |
          var result
          if ("${{ github.event_name }}" == "schedule") {
            result = "scheduled"
          } else if ("${{ github.head_ref }}".match(/^release_/)) {
            result = "release"
          } else {
            result = "normal"
          }
          console.log(result)
          return result
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Debug PATH
      run: |
        echo "PATH"
        echo $PATH | tr ":" "\n"
    - name: Debug PATH with login shell
      shell: bash -l {0}
      run: |
        echo "PATH"
        echo $PATH | tr ":" "\n"
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: Debug PATH without login shell
      run: |
        echo "PATH"
        echo $PATH | tr ":" "\n"
    - name: Debug PATH with login shell
      shell: bash -l {0}
      run: |
        echo "PATH"
        echo $PATH | tr ":" "\n"
    - name: Display initial Python packages
      run: |
        echo "Installed Python packages:"
        pip list
    - name: Display platform and env vars
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make platform env
    - name: Install the package and its dependents
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make install
    - name: Show installed package versions
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        echo "Installed Python packages:"
        pip list
    - name: Development setup
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make develop
    - name: Show installed package versions
      run: |
        echo "Installed Python packages:"
        pip list
    - name: Debug Python files on macos
      if: startsWith(matrix.os, 'macos')
      run: |
        export python_mnu=$(python --version | sed -e 's/Python //')
        export python_mn=${{ matrix.python-version }}
        echo "Directory /Library/Frameworks/Python.framework/Versions/Current:"
        ls -ld /Library/Frameworks/Python.framework/Versions/Current || true
        echo "Directory /Library/Frameworks/Python.framework/Versions/Current/bin:"
        ls -ld /Library/Frameworks/Python.framework/Versions/Current/bin || true
        echo "Files in /Library/Frameworks/Python.framework/Versions/Current/bin/:"
        ls -l /Library/Frameworks/Python.framework/Versions/Current/bin/ || true
        echo "Directory /Library/Frameworks/Python.framework/Versions/${python_mn}:"
        ls -ld /Library/Frameworks/Python.framework/Versions/${python_mn} || true
        echo "Directory /Library/Frameworks/Python.framework/Versions/${python_mn}/bin:"
        ls -ld /Library/Frameworks/Python.framework/Versions/${python_mn}/bin || true
        echo "Files in /Library/Frameworks/Python.framework/Versions/${python_mn}/bin/:"
        ls -l /Library/Frameworks/Python.framework/Versions/${python_mn}/bin/ || true
        echo "Directory /Users/runner/hostedtoolcache/Python/${python_mnu}:"
        ls -ld /Users/runner/hostedtoolcache/Python/${python_mnu} || true
        echo "Directory /Users/runner/hostedtoolcache/Python/${python_mnu}/arm64/bin:"
        ls -ld /Users/runner/hostedtoolcache/Python/${python_mnu}/arm64/bin || true
        echo "Files in /Users/runner/hostedtoolcache/Python/${python_mnu}/arm64/bin/:"
        ls -l /Users/runner/hostedtoolcache/Python/${python_mnu}/arm64/bin/ || true
    - name: Debug coveralls without login shell
      run: |
        echo "Python import location for coveralls:"
        python -c "import coveralls; print(coveralls.__file__)"
        echo "Which coveralls:"
        which -a coveralls
        echo "Coveralls script:"
        cat $(which coveralls)
        echo "Invoke coveralls --version:"
        coveralls --version
    - name: Debug coveralls with login shell
      shell: bash -l {0}
      run: |
        echo "Python import location for coveralls:"
        python -c "import coveralls; print(coveralls.__file__)"
        echo "Which coveralls:"
        which -a coveralls
        echo "Coveralls script:"
        cat $(which coveralls)
        echo "Invoke coveralls --version:"
        coveralls --version
    - name: Show package dependency tree
      run: |
        echo "Package dependency tree of installed Python packages:"
        python -m pipdeptree --all
    - name: Run build
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make build
    - name: Run builddoc
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make builddoc
    - name: Run check
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make check
    - name: Run ruff
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make ruff
    - name: Run pylint
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make pylint
    - name: Run unit test
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
        # TESTCASES: test_cim_obj.py
      run: |
        make test
    - name: Run end2end_mocked test
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
        # TESTCASES: test_cim_obj.py
      run: |
        make end2end_mocked
    - name: Send coverage result to coveralls.io
      shell: bash -l {0}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        COVERALLS_PARALLEL: true
        COVERALLS_FLAG_NAME: "${{ matrix.os }},${{ matrix.python-version }},${{ matrix.package_level }}"
        COVERALLS_SERVICE_NAME: github
        COVERALLS_SERVICE_JOB_ID: "${{ github.run_id }}"
        COVERALLS_SERVICE_NUMBER: "${{ github.workflow }}-${{ github.run_number }}"
      run: |
        coveralls
    - name: Run installtest
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make installtest
    - name: Run debuginfo
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make debuginfo
    - name: Run check_reqs
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make check_reqs
    - name: Run safety
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make safety
    - name: Run bandit
      env:
        PACKAGE_LEVEL: ${{ matrix.package_level }}
        RUN_TYPE: ${{ steps.set-run-type.outputs.result }}
      run: |
        make bandit

  test_finish:
    needs: test
    runs-on: ubuntu-latest
    container: python:3-slim
    steps:
    - name: Install coveralls
      run: |
        pip3 install --upgrade coveralls
    - name: Send coverage finish to coveralls.io
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        COVERALLS_SERVICE_NUMBER: "${{ github.workflow }}-${{ github.run_number }}"
      run: |
        coveralls --finish
